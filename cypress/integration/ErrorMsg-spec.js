describe('error messages', () => {
  it('should not have to visit the page alone', () => {
    // we do not have to have a cy.visit here if we run all the tests at once
    expect(true).to.equal(true);
  });

  it('should mainly be a div', () => {
    
  });

  it('should contain "Sorry, something went wrong!"', () => {
    
  });

  it('should contain "Please try again later!"', () => {
    
  });
});
